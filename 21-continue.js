// for loop to print even numbers only
for (let i =0; i<10; i++) {
    // check if the number is odd
    if (i % 2 !=0) {
        // if it is, skip to the next round of the loop
        continue; // move the red arrow
        // to the start of the loop
    }
    console.log(i);
}